{
  "name": "stampace-charming-essential",
  "version": "1.0.0",
  "description": "Guest book digitale per Stampace Charming — PWA mobile-first.",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html app.js icons.js theme-utils.js content.js supabase.js security.js styles.css host.html host.js host.css manifest.json sw.js template.json .nojekyll dist/ && cp -R guest img dist/",
    "check": "npm run check:js && npm run check:json",
    "check:js": "node --check app.js && node --check host.js && node --check content.js && node --check supabase.js && node --check security.js && node --check sw.js && node --check icons.js && node --check theme-utils.js",
    "check:json": "node -e \"JSON.parse(require('fs').readFileSync('template.json', 'utf8')); JSON.parse(require('fs').readFileSync('manifest.json', 'utf8'))\"",
    "lint": "eslint .",
    "test": "node --experimental-test-module-mocks --test tests/security.test.mjs tests/content.test.mjs"
  },
  "devDependencies": {
    "eslint": "^10.4.1"
  }
}
